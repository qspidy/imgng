{
  "name": "imgul",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "cloudflare": {
    "bindings": {
      "BASIC_PASS": {
        "description": "Set a password for HTTP Basic auth on the upload endpoint."
      },
      "BASIC_USER": {
        "description": "Optional username for HTTP Basic auth. Default is `user`."
      },
      "API_PATH_PREFIX": {
        "description": "Path served by the upload API, for example `upload` or `api/upload`. Use an empty value to serve from `/`."
      },
      "IMAGES": {
        "description": "R2 bucket used to store uploaded images. Cloudflare can provision this during template deployment."
      },
      "PUBLIC_BASE_URL": {
        "description": "Optional public base URL used in returned image URLs. Leave empty to use the Worker origin by default, or set a custom domain later."
      },
      "PUBLIC_PATH_PREFIX": {
        "description": "Object key prefix used for stored files and returned URLs. Default is `images`, but you can change it to values like `abc`."
      }
    }
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.12.0"
  }
}
